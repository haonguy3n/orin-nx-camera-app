host-ui/build/
embedded/build/

oe-*

# Provisioned certs/keys must never be committed (scripts/provision-device-cert.sh)
*.key
*.crt
*.csr
*.srl

# Face-detection model blob (download into the recipe files/ dir, see its README)
*.onnx
