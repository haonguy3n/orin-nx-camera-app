# Drop-in for dnsmasq.service: start after the USB gadget so usb0 exists
# (bind-dynamic in /etc/dnsmasq.d/usb0.conf covers re-plug at runtime).
[Unit]
After=usb-gadget.service
Wants=usb-gadget.service
